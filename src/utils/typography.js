import Typography from 'typography';

export default new Typography({
    baseFontSize: '18px',
    baseLineHeight: 1.45,
    headerFontFamily: ['limelight', 'serif'],
    headerWeight: 700,
    bodyFontFamily: ['limelight', 'sans-serif']
});